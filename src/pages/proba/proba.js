import Header from "../../components/header/header";

export default function Proba(props) {
   const {chartDatas, userData, postData} = props;
   //return (`${Posts(props)}`);
   return (`${Header(userData)}`);
   //return (`${LeftCharts(props)}`)
   //return (`${MainComponent(props)}`)
}
