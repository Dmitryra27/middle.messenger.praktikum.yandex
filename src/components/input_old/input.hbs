
        <input      id="{{id}}"
                    type="{{ type }}"
                    name="{{ inputName }}"
                    placeholder = "{{ placeholder }}"
                    required="{{ required }}"
                    autofocus="{{ autofocus }}"
                    class="{{ className }}"
                    minlength="{{ minLength }}"
                    maxlength="{{ maxLength }}"
                    autocomplete="{{autocomplete}}"
            />








